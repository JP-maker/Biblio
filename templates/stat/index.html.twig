{% extends 'base.html.twig' %}

{% block title %}Statistiques{% endblock %}

{% block charts %}
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col s10 offset-s1 m8 offset-m2 white z-depth-3 pb-10">
            <h3 class="center-align">Vos Statistiques</h3>
            <div class="row">
                <div class="col s12 m6">
                    <div id="top_book" class="chart"></div>
                    <div id="barTopAutor"></div>

                </div>
                <div class="col s12 m5">
                    <div id="bad_book"></div>
                    <div id="barBadAutor"></div>
                </div>
            </div>
            <div class="row">
               <div class="col s12 m12">
                   <h5 class="center-align">Pourcentage de demande de prolongation de prêt : <span class="blue-text">{{ loan }}</span></h5>
               </div>
            </div>

        </div>
    </div>
    <script type="text/javascript">
        {{ gc_draw(piecharttopbook, 'top_book') }}
        {{ gc_draw(piechartbadbook, 'bad_book') }}
        {{ gc_draw(barTopAutor, 'barTopAutor') }}
        {{ gc_draw(barBadAutor, 'barBadAutor') }}
    </script>

{% endblock %}

{% block javascripts %}
    <script type="text/javascript">

        $(window).resize(function(){
            {{ gc_draw(piecharttopbook, 'top_book') }}
            {{ gc_draw(piechartbadbook, 'bad_book') }}
            {{ gc_draw(barTopAutor, 'barTopAutor') }}
            {{ gc_draw(barBadAutor, 'barBadAutor') }}
        });

    </script>
{% endblock %}